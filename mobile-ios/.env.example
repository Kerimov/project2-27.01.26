# Production API (обязательно для TestFlight / App Store)
# EXPO_PUBLIC_API_BASE_URL=https://api.example.com

# Локальная разработка: Metro подставит IP Mac из QR (см. api/client.ts).
# Для симулятора без .env используется http://localhost:3000
