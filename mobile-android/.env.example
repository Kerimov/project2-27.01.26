# Production API (Vercel)
EXPO_PUBLIC_API_BASE_URL=https://project2-27-01-26.vercel.app

# Локальная разработка: закомментируйте строку выше и запустите `npm run dev` в корне репозитория.
# Metro на физическом устройстве подставит IP Mac из QR (см. api/client.ts).
